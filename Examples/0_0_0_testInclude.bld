int g_Flag

struct locals{

    double[x,y,z]
}

decl testFunction(){
  
    int expInt = 10;
    double expDb = 5.2;

    //
    for(int i = 0, i < 10, i++){

        static{ slog("Starte Schleife") }
        if(i == 7){ return i }
    } \
    defer { slog("Verlasse Schleife") }
}